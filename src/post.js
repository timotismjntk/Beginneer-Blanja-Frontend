/* eslint-disable no-unused-expressions */
import React from 'react';

const post = (props) => {
    <div>
        <div>{props.name}</div>
        <div>{props.episode}</div>
    </div>
}

export default post;